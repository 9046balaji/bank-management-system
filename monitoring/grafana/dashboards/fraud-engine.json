{
  "annotations": { "list": [] },
  "editable": true,
  "fiscalYearStartMonth": 0,
  "graphTooltip": 0,
  "id": null,
  "links": [],
  "liveNow": false,
  "panels": [
    {
      "gridPos": { "h": 8, "w": 12, "x": 0, "y": 0 },
      "id": 1,
      "title": "Fraud Engine Risk Score Distribution",
      "type": "timeseries",
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "prometheus" },
          "expr": "rate(fraud_evaluations_total[5m])",
          "legendFormat": "Evaluations RPS"
        }
      ]
    },
    {
      "gridPos": { "h": 8, "w": 12, "x": 12, "y": 0 },
      "id": 2,
      "title": "Circuit Breaker Fallback Rate (Rule-Based Fallback)",
      "type": "timeseries",
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "prometheus" },
          "expr": "rate(fraud_fallback_total[5m]) / rate(fraud_evaluations_total[5m]) * 100",
          "legendFormat": "Fallback Rate %"
        }
      ]
    },
    {
      "gridPos": { "h": 8, "w": 24, "x": 0, "y": 8 },
      "id": 3,
      "title": "Fraud Inference Latency (p50, p95, p99 Target < 10ms)",
      "type": "timeseries",
      "targets": [
        {
          "datasource": { "type": "prometheus", "uid": "prometheus" },
          "expr": "histogram_quantile(0.95, sum(rate(fraud_inference_latency_ms_bucket[5m])) by (le))",
          "legendFormat": "p95 Latency (ms)"
        },
        {
          "datasource": { "type": "prometheus", "uid": "prometheus" },
          "expr": "histogram_quantile(0.99, sum(rate(fraud_inference_latency_ms_bucket[5m])) by (le))",
          "legendFormat": "p99 Latency (ms)"
        }
      ]
    }
  ],
  "refresh": "10s",
  "schemaVersion": 38,
  "style": "dark",
  "tags": ["aurabank", "ai", "fraud", "slo"],
  "time": { "from": "now-1h", "to": "now" },
  "timepicker": {},
  "timezone": "browser",
  "title": "AuraBank — AI Fraud Engine Performance & Drift",
  "uid": "fraud-engine-overview"
}
