/**
 * System Config Context Re-export
 * Ensures a single Context instance is shared across both /contexts and /src/contexts imports.
 */

export * from '../src/contexts/SystemConfigContext';
export { default } from '../src/contexts/SystemConfigContext';
